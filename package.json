{
  "name": "viteup-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "Viteup Monorepo",
  "engines": {
    "node": "^18.0.0 || ^20.0.0 || >=22.0.0"
  },
  "type": "module",
  "scripts": {
    "check:write": "pnpm -r check:fix",
    "prepare": "pnpm -r build && pnpm t",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "~22.10.2",
    "publint": "^0.2.12",
    "typescript": "^5.7.2",
    "vite": "^6.0.6",
    "vitest": "^2.1.8"
  }
}

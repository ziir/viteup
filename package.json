{
	"name": "viteup-monorepo",
	"version": "0.0.1",
	"description": "Viteup Monorepo",
	"engines": {
		"node": "^18.0.0 || >=20.0.0"
	},
	"scripts": {
		"check:fix": "pnpm -r check:fix",
		"prepare": "pnpm -r build && pnpm t",
		"test": "vitest --run"
	},
	"devDependencies": {
		"@biomejs/biome": "1.6.4",
		"@types/node": "^20.11.14",
		"typescript": "^5.4.5",
		"vite": "^5.0.12",
		"vitest": "^1.2.2"
	}
}
